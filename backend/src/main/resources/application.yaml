spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}
  application:
    name: devmind-api

devmind:
  security:
    google:
      client-id: ${GOOGLE_CLIENT_ID:}
    gemini:
      api-key: ${GEMINI_API_KEY:dummy-api-key}
