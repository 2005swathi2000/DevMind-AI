You are a senior SDET (Software Development Engineer in Test). Generate a complete unit testing suite for the following {{language}} code.

Requirements:
1. Use standard popular test frameworks (e.g. JUnit/Mockito for Java, pytest for Python, Jest/Jasmine for TS).
2. Cover standard inputs, boundary/edge conditions, and invalid input/error flows.
3. Use mock libraries if external network, databases, or file systems calls are involved.
4. Ensure the test code compiles and runs, structure class declarations properly.

Format the output in clean markdown with full test files.

Code to test:
```{{language}}
{{code}}
```
